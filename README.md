# 💼 AI Portfolio Builder (Foliomind)

Веб-приложение, которое позволяет пользователям создавать персональные сайты-портфолио с помощью искусственного интеллекта. Пользователь вводит свои навыки, опыт и проекты, а AI автоматически генерирует профессиональное портфолио.

## ✨ Ключевые особенности (MVP)

* **🔐 Безопасная аутентификация:** Регистрация и вход с использованием **RS256** (асимметричные ключи) и **Argon2** (хеширование паролей).
* **🤖 AI-генерация контента:** *[В РАЗРАБОТКЕ]* Автоматическое написание разделов "Обо мне", "Навыки" и "Описание проектов" с помощью OpenAI.
* **🎨 Выбор шаблона:** *[В РАЗРАБОТКЕ]* Пользователи могут выбрать стиль своего портфолио.
* **📥 Экспорт кода:** *[В РАЗРАБОТКЕ]* Возможность скачать готовое портфолио в виде **HTML/CSS**.

## 🧩 Технологический стек

* **Frontend:** **React (Next.js)**
* **Styling:** **TailwindCSS** + **shadcn/ui**
* **Backend:** **FastAPI (Python)**
* **Database:** **PostgreSQL** + **Alembic** (миграции)
* **Security:** **JWT (RS256)** + **Argon2**
* **Containerization:** **Docker** & **Docker Compose**
* **Version Control:** **Git + GitHub**

## 🚀 Локальный запуск (Getting Started)

Для запуска проекта локально с использованием Docker:

### 1. Клонирование и настройка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone [https://github.com/Adilet-tech/ai-portfolio-builder.git](https://github.com/Adilet-tech/ai-portfolio-builder.git)
    cd ai-portfolio-builder
    ```

2.  **Создайте `.env` файл:**
    В корне проекта (`/`) создайте файл `.env` и добавьте в него свои настройки БД:

    ```env
    # Настройки Базы Данных (PostgreSQL)
    POSTGRES_USER=adikus
    POSTGRES_PASSWORD=adikus
    POSTGRES_DB=portfolio_db
    DATABASE_URL=postgresql://adikus:adikus@db:5432/portfolio_db
    
    # Ключ API OpenAI (понадобится позже)
    OPENAI_API_KEY=sk-xxxxxxxxxxxx
    ```

3.  **Сгенерируйте ключи безопасности (RS256):**
    Выполните эти две команды в терминале (в корне проекта), чтобы создать `private_key.pem` и `public_key.pem`:
    ```bash
    openssl genrsa -out private_key.pem 2048
    openssl rsa -in private_key.pem -pubout -out public_key.pem
    ```

4.  **Добавьте ключи в `.gitignore`:**
    Убедитесь, что в вашем `.gitignore` есть строка, чтобы не загружать приватный ключ в репозиторий:
    ```gitignore
    *.pem
    ```

### 2. Запуск Docker

1.  **Соберите и запустите контейнеры:**
    Эта команда соберет образы (frontend, backend) и запустит контейнеры (включая базу данных).
    ```bash
    docker compose up --build
    ```
    *Дождитесь, пока все 3 сервиса (`db`, `backend`, `frontend`) запустятся.*

### 3. Настройка Базы Данных (Alembic)

При первом запуске (или после `docker compose down -v`) база данных будет пустой. **Ее нужно настроить.**

1.  **Откройте НОВЫЙ терминал** (не останавливая `docker compose up`).
2.  **Создайте миграцию** (Alembic сравнит `models.py` с пустой БД):
    ```bash
    docker compose exec backend alembic revision --autogenerate -m "Create initial tables"
    ```
3.  **Примените миграцию** (Это создаст таблицы `user`, `portfolio` и т.д.):
    ```bash
    docker compose exec backend alembic upgrade head
    ```
    *(Если вы получите ошибку `NameError: name 'sqlmodel' is not defined`, добавьте `import sqlmodel` вверху сгенерированного файла миграции в `backend/alembic/versions/` и выполните `upgrade` еще раз).*

### 4. Готово!

* **Frontend** доступен по адресу: `http://localhost:3000`
* **Backend (API)** доступен по адресу: `http://localhost:8000/docs`

## 📁 Структура проекта (Backend)

/backend ├── alembic/ # Файлы миграций Alembic │ ├── versions/ │ └── env.py ├── app/ │ ├── api/ # API-Роутеры │ │ ├── auth.py # (Регистрация, Логин) │ │ └── users.py # (/users/me) │ ├── db.py # Настройка подключения к БД │ ├── dependencies.py # Защита эндпоинтов (get_current_user) │ ├── models.py # SQLModel модели (таблицы) │ ├── schemas.py # Pydantic схемы (DTO) │ ├── security.py # (Argon2, RS256 JWT) │ └── main.py # Главный файл FastAPI │ ├── Dockerfile └── requirements.txt


## API Эндпоинты (MVP)

* `POST /api/v1/auth/register` - Регистрация нового пользователя.
* `POST /api/v1/auth/token` - Получение JWT токена (логин).
* `GET /api/v1/users/me` - (Защищено) Получение данных о текущем пользователе.